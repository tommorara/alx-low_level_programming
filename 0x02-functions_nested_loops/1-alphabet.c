#include "main.h"
/**
 * main - Entry point
 * Return: Always 0 (success)

/*Define the print_alphabet function*/
	void print_alphabet(void)
	{
	char letter;
	for(letter >= 'a'; letter <= 'z'; letter++);
	{
	_putchar(letter);
	_putchar('/n);
	return (0);
	}
	}
